console.log('scriptConnected')

imageButton.onclick = function (e) {
	e.preventDefault();
};

textButton.onclick = function (e) {
  e.preventDefault();
};

galleryButton.onclick = function (e) {
  e.preventDefault();
};
